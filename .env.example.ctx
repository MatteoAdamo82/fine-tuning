purpose: "Template variabili ambiente. Copiare in .env e compilare prima di usare forge."

tensions:
  - "HF_TOKEN richiede write access: anche se non si vuole fare push al Hub,
    il token serve per scaricare modelli gated come Llama. Read-only non basta."
